import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "Gyanarthi College Admissions 2025 | Apply Online for UG & PG Courses",
  description: "Apply online for 2025 admissions at Gyanarthi Media College. Explore UG & PG courses in Mass Communication, BCOM, BA Fine Arts, and Fashion Design. Limited seats!",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
